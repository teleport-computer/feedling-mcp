---
title: Core workflows
description: End-to-end request sequences for Chat, Memory, and Perception.
---

The API reference documents one HTTP operation at a time. These guides document
the state transitions between operations, including encryption boundaries and
asynchronous responses.

Start with [architecture and trust boundaries](/docs/architecture) if you need
the system-level relationship between clients, persistence, the decrypt service,
hosted runners, and external model providers.

<Cards>
  <Card title="Chat" description="Send hosted messages or operate an encrypted resident loop." href="/docs/workflows/chat" />
  <Card title="Memory" description="Write encrypted cards, list an index, and fetch selected content." href="/docs/workflows/memory" />
  <Card title="Perception" description="Report current signals and upload encrypted photo content." href="/docs/workflows/perception" />
</Cards>

All examples use placeholders. Never copy example ciphertext, nonces, wrapped
keys, or credentials into a real integration. Generate envelope values with the
Feedling client encryption implementation.

## Shared encrypted envelope

Chat, Memory, and sensitive Perception payloads reuse a versioned envelope:

```json
{
  "v": 1,
  "id": "CLIENT_GENERATED_UNIQUE_ID",
  "body_ct": "BASE64_CIPHERTEXT",
  "nonce": "BASE64_NONCE",
  "K_user": "BASE64_KEY_WRAPPED_TO_USER",
  "K_enclave": "BASE64_KEY_WRAPPED_TO_ENCLAVE",
  "enclave_pk_fpr": "ENCLAVE_PUBLIC_KEY_FINGERPRINT",
  "visibility": "shared",
  "owner_user_id": "usr_0123456789abcdef"
}
```

`shared` content requires `K_enclave` so the trusted decrypt service can read it
for agent workflows. `local_only` omits `K_enclave`; the service can store the
record but agent-side read paths cannot decrypt its body. `owner_user_id` must
match the authenticated account on operations that validate ownership.

Envelope bodies remain ciphertext in the main API process. Some read-side
operations forward authorized candidates to the enclave and return a limited
decrypted projection. Read each workflow's privacy notes before choosing an
endpoint.

## Operator release-evaluation workflow

The repository also contains a formal live workflow for testing one candidate
already deployed to the protected test environment. It is deliberately separate
from the public API workflows above:

```text
serialize deployment
  -> provision 8 synthetic accounts (or 24 at release depth)
  -> verify backend deployment identity and per-account V2 user path
  -> import one hash-bound persona/memory snapshot
  -> verify backend deployment identity again and issue readiness receipt
  -> issue pre-run deployment receipt
  -> run isolated live conversations with an OAuth-backed Codex judge
  -> issue post-run deployment receipt
  -> checkpoint and clean the full pool, then issue cleanup receipt
  -> finalize the arm
  -> publish an allowlisted candidate summary
```

Eight accounts is the default smoke depth; 24 accounts is release depth (eight
accounts times three repetitions). Neither is a public API limit. The runner can
process those independent account sessions concurrently without creating a
container per conversation. The deployed backend SHA and authenticated
`hosted_resident` mode/version are bound into the result; worker binary SHA and
live worker count remain explicitly unavailable. A separate manual library flow
can compare disjoint baseline and candidate arms when both retain the same
persona, source, route, evaluator, judge, and coverage contracts.

Only allowlisted aggregate outputs are suitable for publication. The trusted
supervisor retains deployment and cleanup authority and protects receipts from
runner writes; raw conversations and imported plaintext stay private. See the
[architecture trust boundary](/docs/architecture#evaluation-trust-and-receipt-boundary)
and the [self-hosting upgrade gate](/docs/self-hosting#optional-formal-upgrade-regression-gate)
for the required isolation.
