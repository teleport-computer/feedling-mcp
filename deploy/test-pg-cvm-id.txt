# feedling-io-db-test 的 CVM uuid——首次开通后填入（一行、无注释前缀）。
# 首次开通流程见 deploy/DEPLOYMENTS.md「TEE Postgres」；
# 本文件为空时 pg-deploy workflow 会 fail-closed（绝不静默新建 CVM）。
# app_id = ca2317945ce4c95f5d5a9df60676a74d59197dcc（workspace: amiller-users-projects）
# 用 uuid 而非 app_id：`phala deploy --wait` 的状态轮询只认 UUID，传 app_id 会
# 假报 Invalid UUID / 超时（其实 update 已生效）。
bf4e1583-ef95-457a-9360-13487c1a9def
