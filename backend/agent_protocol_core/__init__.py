"""模型协议层的纯共享判据 —— 与「记忆」无关，谁都能用。

这里放的是**处理模型原始输出**的通用能力：思维链剥离、协议残片/泄漏识别。
聊天主链路、工具循环、主动唤醒、记忆落卡都要用它，所以它不属于任何一个领域。

## 为什么单独一个包

内核提取的第一版把 ``self_thinking`` 与 ``protocol_leak`` 搬进了
``memory_garden``（因为卡片字段校验依赖它们）。结果是**普通聊天反向依赖了
记忆包** —— 将来替换或独立发布 Memory Garden 时，聊天的输出解析会跟着断。
（codex review 2026-08-14 指出。）

依赖方向现在是单向的：

    memory_garden  ──►  agent_protocol_core
    聊天 / 工具循环 ──►  agent_protocol_core

两边都依赖这个中立包，彼此不依赖。

## 边界

和 ``memory_garden`` 一样：纯函数、不 import 任何 io 模块。
运行时开关（环境变量）由宿主读取后传入，不在这里解析。
"""
