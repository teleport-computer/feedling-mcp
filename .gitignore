backend/__pycache__/
**/__pycache__/
.pytest_cache/

# Foundry build artifacts — contracts/lib is a vendored dep we do commit
contracts/out/
contracts/cache/
contracts/broadcast/

# Local env files
.env
deploy/.env
contracts/.env

# macOS
.DS_Store

# Xcode

# Swift build artifacts
ios/FeedlingDCAP/.build/
ios/FeedlingDCAP/.swiftpm/
ios/**/Package.resolved
.gstack/

# dcap-qvl Rust build outputs (committed XCFramework is what the app links)
ios/vendor/dcap-qvl/target/
**/*.rlib
**/*.rmeta

# Claude Code session scratch (task locks, transient state)
.claude/

# Local agent mailbox runtime (messages, inboxes, pane config)
.agents/
buildServer.json
deploy/backups/

# Local dev virtualenvs (created for running the test suite / audit sessions)
.venv/
.venv-audit/
.superpowers/
